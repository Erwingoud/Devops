meta {
  name: Create invalid recipe
  type: http
  seq: 4
}

post {
  url: {{recipeApiUrl}}/recipes
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "A",
    "cuisine": "",
    "prepTimeMinutes": 0
  }
}

tests {
  test("fails with validation error", function () {
    expect(res.status).to.equal(400);
    expect(res.body.error).to.exist;
  });
}