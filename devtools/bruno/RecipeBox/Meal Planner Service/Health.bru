meta {
  name: Health
  type: http
  seq: 1
}

get {
  url: {{mealPlannerUrl}}/health
  body: none
  auth: none
}

tests {
  test("returns meal planner health", function () {
    expect(res.status).to.equal(200);
    expect(res.body.service).to.equal("meal-planner-service");
  });
}